# Report Variables for Vanshaj_Report.tex
# Fill in the actual values, then use find-and-replace in the LaTeX file

# Performance Metrics (percentages without % sign, e.g., 65 or 12.5)
level1_acc=
level2_acc=

# Training Time Metrics (include units, e.g., "5 minutes" or "2.5 hours")
sft_time=
rl_time=
total_time=

# Instructions:
# 1. Fill in all values above
# 2. In Vanshaj_Report.tex, search for: \texttt{level1\_acc}
#    Replace with just the number (no \texttt{} wrapper): level1_acc value
# 3. Repeat for all other variables:
#    - Search: \texttt{level2\_acc}  → Replace: level2_acc value
#    - Search: \texttt{sft\_time}    → Replace: sft_time value
#    - Search: \texttt{rl\_time}     → Replace: rl_time value
#    - Search: \texttt{total\_time}  → Replace: total_time value
